namespace Allup.Helpers
{
    public class Helper
    {
    }
    public enum UserRoles
    {
        SuperAdmin,
        Admin,
        Member,

    }
    public enum OrderStatus
    {
        Pending,
        Shipped,
    }
}
